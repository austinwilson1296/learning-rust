## Future Project Idea: Rust Learnings CLI App

A dedicated CLI tool for tracking the 100 days of Rust journey. Features:
- Terminal pomodoro timer with ASCII art
- Time analytics (how long spent per session/day)
- Note taking (capture thoughts while learning)
- Streak tracker (days hitting the 2 hour goal)

## Topics covered

- Using derive traits to add build cli functionality with the clap crate
- Spinning up local sqlite3 db | adding a table | simple crud operations
- modular project structure utilizing sub packages
- Found out what mod.rs is idomatic way to reference internal functionality across think **init**.py for module declaration in python
- Utilized both enums and structs. publicly and internally
- Match keyword (all possible cases must be covered when using it with things like structs and enums)
- Still fuzzy on result
- In this project specifically havent implicity worked much with ownership or borrowing 
